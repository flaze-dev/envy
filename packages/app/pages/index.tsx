

// Components
const IndexPage = () => {

  // Render
  const render = () => {
    return <div className="w-full h-full flex items-center justify-center">
      <span className="text-2xl text-gray-900 font-bold">Envy</span>
    </div>;
  }

  return render();
};

export default IndexPage;
